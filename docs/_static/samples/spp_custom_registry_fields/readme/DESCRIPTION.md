Adds custom fields to the OpenSPP individual registry:

- **Education Level**: Tracks the highest education level using a vocabulary code
- **Head of Household**: Boolean flag indicating household head status

This module serves as an example for developers learning to extend the OpenSPP registry.
